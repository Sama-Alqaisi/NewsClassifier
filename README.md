# NewsClassifier
Repository for a news classification project. Features data preprocessing, model building (KNN, Decision Tree, GNB), and evaluation. Uses various embeddings (Bag of Words, TF-IDF, Word2Vec, FastText, BERT) for improved text representation. Includes Jupyter Notebooks and a presentation on methods and results.
